import {
  Description,
  Meta,
  Story,
  Canvas,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import Logo from '.';

<Meta
  title="Atom/Logo"
  component={Logo}
  argTypes={{
    text: {
      control: 'text',
    },
    color: {
      control: 'color',
    },
    colorHover: {
      control: 'color',
    },
    cursor: {
      control: 'text',
    },
    fontFamily: {
      control: 'text',
    },
    fontStyle: {
      control: 'text',
    },
    gradient: {
      control: 'text',
    },
    gradientHover: {
      control: 'text',
    },
    isGradient: {
      control: 'boolean',
    },
    lineHeight: {
      control: 'text',
    },
    size: {
      control: 'text',
    },
    textWidth: {
      control: 'text',
    },
    weight: {
      control: 'text',
    },
  }}
/>

export const LogoTemplate = (args) => <Logo {...args} />;

### Control

<Canvas
  mdxSource={`<Logo text="Anibook" color="#FFD500" colorHover="#00D2D3" />`}
>
  <Story
    name="Default"
    args={{
      text: 'Anibook',
      color: '#ffd500',
      colorHover: '#00d2d3',
      gradient: '-webkit-linear-gradient(90deg, #ffd500, #ff1493)',
      gradientHover: '-webkit-linear-gradient(90deg, #00d2d3, #2e86de)',
    }}
  >
    {LogoTemplate.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable of={Logo} />
